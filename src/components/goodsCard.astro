---
const { href, title, body, thumbnail, price } = Astro.props;
---

<article>
  <a href={href}>
    <img src=`/07_ecsite/images/${thumbnail[0]}` alt={title} />

    <h2>{title}</h2>

    <!-- <button class="sold-out-btn">Sold Out</button> -->
    <p>{body}</p>
  </a>
  <h3>¥{price}</h3>
</article>

<style lang="scss">
  h2 {
    max-width: 100%;
    margin: 0 auto;
    font-size: 18px;
  }

  .sold-out-btn {
    font-size: 18px;
    background-color: #d9534f;
    color: white;
    border: none;
    border-radius: 5px;
    margin-top: 15px;
    padding: 5px 20px;
    font-size: 1.2em;
    font-weight: bold;
    cursor: not-allowed;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    transition:
      background-color 0.3s,
      transform 0.3s;
  }

  .sold-out-btn:hover {
    background-color: #c9302c;
  }
  article {
    max-width: 96%;
    margin-left: auto;
    margin-right: auto;
    a {
      text-decoration: none;
    }
  }

  img {
    margin-top: 60px;
    max-width: 100%;
    margin-left: auto;
    margin-right: auto;
  }
  body {
    margin: 0;
  }

  @media (max-width: 600px) {
    h2 {
      font-size: 18px;
    }

    img {
      margin-top: 20px;
    }
  }
</style>
