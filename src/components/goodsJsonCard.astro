---
import goods from "../data/goods.json";
console.log(goods);
---

<ul>
  {goods.map((list) =>
     <li>{list.name}</li>
     <img src=`/07_ecsite/images/${list.itemphotos[0]}` />
     <p>{list.text}</p>
  )}
</ul>
<style lang="scss"></style>
